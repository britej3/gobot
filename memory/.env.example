# SimpleMem Configuration for GOBOT
# Copy this file to .env and fill in your values

# ============================================================================
# OpenRouter API (LLM) - FREE TIER BY DEFAULT
# ============================================================================
# Get your keys from: https://openrouter.ai/keys (free tier available!)
# No credit card required for free models
#
# TIP: Create two free accounts for rate limit resilience!
#      When primary key is rate limited, backup key is used automatically.

# Primary API Key (required)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Backup API Key (optional - for rate limit resilience)
# Create a second OpenRouter account to get this key
# Leave empty or remove if you only have one key
OPENROUTER_API_KEY_BACKUP=your-backup-api-key-here

# OpenRouter Base URL
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: Set custom referer for OpenRouter analytics
OPENROUTER_REFERER=https://github.com/gobot
OPENROUTER_TITLE=GOBOT Trading Bot

# ============================================================================
# LLM Model - FREE TIER MODELS (no cost!)
# ============================================================================
# Default uses free models. Automatic fallback if rate limited.
# 
# FREE MODELS AVAILABLE:
#   meta-llama/llama-3.1-8b-instruct:free   (DEFAULT - best for trading)
#   meta-llama/llama-3.2-3b-instruct:free   (faster, simpler tasks)
#   google/gemma-2-9b-it:free               (good alternative)
#   mistralai/mistral-7b-instruct:free      (solid performer)
#   qwen/qwen-2-7b-instruct:free            (multilingual)
#   microsoft/phi-3-mini-128k-instruct:free (long context)
#
# PAID MODELS (if you want to upgrade):
#   openai/gpt-4o-mini                      (~$0.15/1M tokens)
#   anthropic/claude-3.5-sonnet             (~$3/1M tokens)
#
# Leave empty to use default free model:
LLM_MODEL=meta-llama/llama-3.1-8b-instruct:free

# ============================================================================
# Ollama Embeddings (Local)
# ============================================================================
# Run: ollama pull nomic-embed-text
OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_MODEL=ollama:nomic-embed-text
EMBEDDING_DIMENSION=768

# Alternative embedding models:
# ollama:mxbai-embed-large (1024 dims)
# ollama:all-minilm (384 dims)
# ollama:snowflake-arctic-embed (1024 dims)

# ============================================================================
# Database
# ============================================================================
LANCEDB_PATH=./memory/lancedb_data

# ============================================================================
# Memory Settings
# ============================================================================
TRADE_MEMORY_RETENTION_DAYS=90
MARKET_MEMORY_RETENTION_DAYS=30

# ============================================================================
# OpenInference Tracing (Optional - for AI observability)
# ============================================================================
# Compatible with Arize Phoenix, LangSmith, etc.
ENABLE_OPENINFERENCE=false
OPENINFERENCE_ENDPOINT=http://localhost:6006/v1/traces
